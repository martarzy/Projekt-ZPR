namespace view {
    /**
     * Represents an object which stores information about a player.
     */
    export class PlayerDTO {
        username: string;
        cash: number;
        active: boolean;
        color: string; 
    }
}